void main(){
  //continue keyword
  //using labels
  
  for(int i = 1; i<=10; i++){
    if(i%2==0){
      continue; //skip that one code and continue the loop
    }
    
    //print(i);
    
  }
  
  
  outer: for(int j =1; j<=3; j++){
    for(int h =1; h<=3; h++){
      if(j==2 && h==2){
         continue outer;
      }
      print ("$j, $h");
    }
  }
}
